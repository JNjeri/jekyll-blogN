{%  for node in pages_list %}

	{% if group == null or group == node.group %}
		{% if pages.url == node.url%}

	    {%  endif %} 
	    <li>
	    	<a href="{{node.url}}" class="selected">{{node.navigationName}}</a>
	    </li>
	    {% else %}
	    	<a href="{{node.url}}">{{node.navigationName}}</a></li>
	{%  endif %}

{% endfor%}
{% assign pages_list = nil%}
{% assign group = nil%}